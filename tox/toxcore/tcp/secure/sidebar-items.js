initSidebarItems({"struct":[["Channel","Encrypt TCP packets with credentials. Increment `sent_nonce` after data was encrypted. increment `recv_nonce` after data was decrypted."],["Session","A Session is created on both sides. Its PK and Nonce is sent from a client to server via handshake. A server creates its own `Session`, creates `Channel` and replies with its Session PK and Nonce to the client, the client creates `Channel`."]]});